# Celebrity-Look-Alike
We present an end-to-end face recognition system built on the CelebA dataset. The system identifies which of 50 known identities a given face most resembles, and is deployed as a web application accepting both file uploads and live webcam captures. Our pipeline pairs a ConvNeXt-Small backbone with an ArcFace classifier , all fine-funed.
